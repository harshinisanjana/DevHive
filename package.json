{
  "name": "devhive-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp dist/index.html ../ && cp -r dist/assets ../ && cd ../backend && npm install",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  }

}
