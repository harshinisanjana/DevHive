{
  "name": "devhive-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx vite build && cp -r dist/* ../ && cd ../backend && npm install",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  }


}
